import React from 'react'
import ReactDOM from 'react-dom/client'
import { PrototypeWrapper } from './pages/PrototypeWrapper'
import './styles/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PrototypeWrapper />
  </React.StrictMode>,
)
